const usersControllers = require('./users-controllers');
const authControllers = require('./auth-controllers');
const growlsControllers = require('./growls-controllers');
const likesControllers = require('./likes-controllers');

module.exports = {
  usersControllers,
  authControllers,
  growlsControllers,
  likesControllers
}